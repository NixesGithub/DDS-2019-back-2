package Notificador;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

import grupo1.utn.frba.dds.Atuendo;

public class AtuendoAMostrar {
	public List<String> superior=new ArrayList();
	public List<String> accesorio=new ArrayList();
	public List<String> inferior=new ArrayList();
	public List<String> calzado=new ArrayList();
	
	/*
 public AtuendoAMostrar(Atuendo unAtuendo){
	 
	 accesorio.addAll(unAtuendo.accesorios.stream().map(prenda->prenda.tipo.descripcion).collect(Collectors.toList()));
	 inferior.addAll(unAtuendo.inferior.stream().map(prenda->prenda.tipo.descripcion).collect(Collectors.toList()));
	 calzado.addAll(unAtuendo.calzado.stream().map(prenda->prenda.tipo.descripcion).collect(Collectors.toList()));
	 superior.addAll(unAtuendo.superior.stream().map(prenda->prenda.tipo.descripcion).collect(Collectors.toList()));
 }
 */
}
